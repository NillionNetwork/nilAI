# Intel TDX integration with NilAI

To add the integration, we need the following:

```shell
# Download required packages
go get <package>
# Ensure dependencies are present
go mod tidy
```

```shell
go build
```
